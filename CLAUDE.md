# Claude Code — AI Use Optimizer

Read **`AGENTS.md`** at the repository root first. It is the single canonical instruction set for
this project.

**Claude-specific:** Most of the tree is prompts and docs; **`promptfill/`** is a small Python CLI
with pytest. Dev hooks use **uv** + pre-commit (see `AGENTS.md` §4).
