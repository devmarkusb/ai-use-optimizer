# Claude Code — AI Use Optimizer

Read **`AGENTS.md`** at the repository root first. It is the single canonical instruction set for
this project.

**Claude-specific:** This tree is prompt-and-docs only. Dev hooks use **uv** + pre-commit (see
`AGENTS.md` §4); there is no application build or test suite.
