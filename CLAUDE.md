# Claude Code — AI Use Optimizer

Read **`AGENTS.md`** at the repository root first. It is the single canonical instruction set for this project.

**Claude-specific:** This tree is prompt-and-docs only. There is no `package.json`, Python env, or CI to infer—if a task needs build or test commands, they are not defined here unless the user adds tooling and updates `AGENTS.md`.
