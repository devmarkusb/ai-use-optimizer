[project]
name = "ai-use-optimizer"
version = "0.0.0"
description = "Dev tooling only — repository content is Markdown prompts and docs."
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = ["pre-commit>=4.0"]

[tool.uv]
default-groups = ["dev"]
